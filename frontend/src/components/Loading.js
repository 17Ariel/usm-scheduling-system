import React from "react";

const Loading = () => {
  return (
    <div className="flex justify-center items-center h-screen">
      <h1 className="text-4xl text-primary-0">Loading.....</h1>
    </div>
  );
};

export default Loading;
